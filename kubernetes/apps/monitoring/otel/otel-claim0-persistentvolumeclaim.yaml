apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: otel-claim0
  name: otel-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
